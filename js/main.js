let sbm = document.querySelector('#contactForm');
sbm?.addEventListener('submit', function (event) {
  // event.preventDefault(); // Prevent default form submission
  alert('Formular wurde erfolgreich gesendet!');
});
